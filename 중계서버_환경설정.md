# 중계 서버 설정 변경 인계서

작성일: 2026-08-11

이 문서의 설정은 현재 프로젝트가 실행되는 중계 서버 환경을 위한 로컬 운영 설정입니다.

## 변경 사항

- Next.js 실행 포트를 `3000`에서 `4000`으로 변경했습니다.
  - 개발: `npm run dev`
  - 운영: `npm start`
  - 접속 주소: `http://localhost:4000`
- VS Code Copilot Proxy 주소를 `http://localhost:3000`으로 변경했습니다.
- 프록시 API를 설치된 확장 기능의 규격에 맞췄습니다.
  - 상태 확인: `GET /health`
  - AI 요청: `POST /copilot-chat`
- 기본 AI 제공자는 `vscode-proxy`, 기본 모델은 `gpt-4o`로 설정했습니다.
- AI 모델 타입에 `gpt-4`, `gpt-3.5-turbo`를 추가했습니다.

## 운영 및 재시작

프로젝트 루트의 `restart-server.bat`를 실행합니다. 배치 파일은 다음 작업을 순서대로 수행합니다.

1. 4000 포트에서 실행 중인 이 프로젝트의 서버 프로세스 확인 및 종료
2. `npm run build` 운영 빌드
3. 빌드 성공 시 `npm start`로 운영 서버 실행

수동 실행이 필요한 경우:

```powershell
npm run build
npm start
```

> `npm run`만 입력하면 서버가 실행되지 않고 등록된 스크립트 목록만 표시됩니다.

## 확인 방법

```text
애플리케이션: http://localhost:4000
앱 상태 확인:  http://localhost:4000/api/health
프록시 상태:   http://localhost:3000/health
```

작업 시점 기준으로 앱과 프록시 모두 HTTP 200 응답을 확인했습니다.

## 관리자 참고사항

- 3000 포트에서는 VS Code 확장 `manual-generator.copilot-proxy`가 실행 중이어야 합니다.
- VS Code와 GitHub Copilot 로그인이 해제되면 프록시 호출이 실패할 수 있습니다.
- 브라우저에 이전 AI 설정이 `localStorage`로 저장되어 있으면 새 기본 설정보다 우선합니다. 이 경우 AI 설정을 초기화하거나 Proxy URL을 `http://localhost:3000`으로 직접 변경하십시오.
- `.env.local`의 `NEXT_PUBLIC_VSCODE_PROXY_URL`에는 기존 `3100` 값이 남아 있으나 현재 AI 설정은 `src/types/ai.ts`의 기본값과 브라우저 `localStorage`를 사용합니다.

## 주요 변경 파일

- `package.json`
- `restart-server.bat`
- `src/types/ai.ts`
- `src/lib/ai/client.ts`
- `src/components/generate/ai-settings-panel.tsx`
- `README.md`

---

## 파일별 변경 전/후 소스

아래 경로는 모두 프로젝트 루트를 기준으로 합니다. 관리자는 각 파일의 해당 부분을 `변경 후` 코드로 교체하면 됩니다.

### 1. `package.json`

Next.js 개발 서버와 운영 서버가 모두 4000 포트를 사용하도록 변경합니다.

#### 변경 전

```json
{
  "scripts": {
    "dev": "next dev --hostname 0.0.0.0 --webpack --port 3000",
    "build": "next build",
    "start": "next start"
  }
}
```

#### 변경 후

```json
{
  "scripts": {
    "dev": "next dev --hostname 0.0.0.0 --webpack --port 4000",
    "build": "next build",
    "start": "next start --port 4000"
  }
}
```

### 2. `src/types/ai.ts`

AI 모델 타입, 기본 모델 및 Proxy URL을 변경합니다.

#### 변경 전

```typescript
export type AiModel =
  | "gpt-4o-mini"
  | "gpt-4o"
  | "gpt-4.1-mini"
  | "gpt-4.1-nano"
  | "gemma4-31b";

export const DEFAULT_AI_SETTINGS: AiSettings = {
  provider: "vscode-proxy",
  model: "gpt-4o-mini",
  proxyUrl: "http://localhost:3100",
  internalBaseUrl: "https://ax.acanet.asia/v1",
  maxTokens: 4096,
  temperature: 0.3,
};
```

#### 변경 후

```typescript
export type AiModel =
  | "gpt-4o-mini"
  | "gpt-4o"
  | "gpt-4"
  | "gpt-3.5-turbo"
  | "gpt-4.1-mini"
  | "gpt-4.1-nano"
  | "gemma4-31b";

export const DEFAULT_AI_SETTINGS: AiSettings = {
  provider: "vscode-proxy",
  model: "gpt-4o",
  proxyUrl: "http://localhost:3000",
  internalBaseUrl: "https://ax.acanet.asia/v1",
  maxTokens: 4096,
  temperature: 0.3,
};
```

### 3. `src/lib/ai/client.ts`

설치된 VS Code Copilot Proxy가 제공하는 실제 요청 경로인 `/copilot-chat`을 사용합니다.

#### 변경 전

```typescript
const url =
  settings.provider === "github"
    ? GITHUB_MODELS_URL
    : `${settings.proxyUrl.replace(/\/$/, "")}/chat/completions`;
```

#### 변경 후

```typescript
const url =
  settings.provider === "github"
    ? GITHUB_MODELS_URL
    : `${settings.proxyUrl.replace(/\/$/, "")}/copilot-chat`;
```

### 4. `src/components/generate/ai-settings-panel.tsx`

설치된 Proxy에는 `/v1/models`가 없으므로 `/health`로 상태를 확인하고 지원 모델 목록을 앱에서 제공합니다.

#### 변경 전: Proxy 모델 타입

```typescript
interface ProxyModel {
  family: string;
  id: string;
}
```

#### 변경 후: Proxy 모델 타입 및 지원 모델

```typescript
interface ProxyModel {
  family: string;
  id: string;
}

const COPILOT_PROXY_MODELS: ProxyModel[] = [
  { family: "gpt-4o", id: "gpt-4o" },
  { family: "gpt-4", id: "gpt-4" },
  { family: "gpt-3.5-turbo", id: "gpt-3.5-turbo" },
];
```

#### 변경 전: 모델 조회

```typescript
const base = proxyUrl.replace(/\/$/, "");
const res = await fetch(`${base}/v1/models`, {
  signal: AbortSignal.timeout(4000),
});
if (!res.ok) throw new Error(`HTTP ${res.status}`);
const json = await res.json();
const seen = new Set<string>();
const items: ProxyModel[] = (json.data ?? [])
  .map((m: Record<string, string>) => ({
    family: m.family ?? m.id,
    id: m.id,
  }))
  .filter((m: ProxyModel) =>
    m.family.startsWith("gpt-4") || m.family.startsWith("gpt-5")
  )
  .filter((m: ProxyModel) => {
    if (seen.has(m.family)) return false;
    seen.add(m.family);
    return true;
  });
```

#### 변경 후: Proxy 상태 확인 및 모델 구성

```typescript
const base = proxyUrl.replace(/\/$/, "");
const res = await fetch(`${base}/health`, {
  signal: AbortSignal.timeout(4000),
});
if (!res.ok) throw new Error(`HTTP ${res.status}`);
const json = await res.json();
if (json.status !== "ok") throw new Error("Proxy is not ready");

const seen = new Set<string>();
const items: ProxyModel[] = COPILOT_PROXY_MODELS
  .map((m) => ({ family: m.family, id: m.id }))
  .filter((m: ProxyModel) =>
    m.family.startsWith("gpt-4") || m.family.startsWith("gpt-5")
  )
  .filter((m: ProxyModel) => {
    if (seen.has(m.family)) return false;
    seen.add(m.family);
    return true;
  });
```

#### 변경 전: Proxy URL 입력 안내

```tsx
<Input
  id="proxy-url"
  type="text"
  value={settings.proxyUrl}
  onChange={(e) => onChange({ proxyUrl: e.target.value })}
  placeholder="http://localhost:3100"
/>
```

#### 변경 후: Proxy URL 입력 안내

```tsx
<Input
  id="proxy-url"
  type="text"
  value={settings.proxyUrl}
  onChange={(e) => onChange({ proxyUrl: e.target.value })}
  placeholder="http://localhost:3000"
/>
```

### 5. `restart-server.bat` 신규 생성

기존 파일은 없으며, 프로젝트 루트에 아래 내용으로 새 파일을 생성합니다.

```bat
@echo off
setlocal

cd /d "%~dp0"

echo [1/3] Checking port 4000...
powershell.exe -NoProfile -ExecutionPolicy Bypass -Command ^
  "$project = [IO.Path]::GetFullPath('%~dp0').TrimEnd('\');" ^
  "$listener = Get-NetTCPConnection -State Listen -LocalPort 4000 -ErrorAction SilentlyContinue | Select-Object -First 1;" ^
  "if (-not $listener) { exit 0 };" ^
  "$process = Get-CimInstance Win32_Process -Filter ('ProcessId = ' + $listener.OwningProcess);" ^
  "if ($process.CommandLine -notlike ('*' + $project + '*') -or $process.CommandLine -notlike '*next*') { Write-Error ('Port 4000 is used by another process (PID ' + $listener.OwningProcess + ').'); exit 2 };" ^
  "$target = $null; $current = $process;" ^
  "while ($current -and $current.ParentProcessId) { $parent = Get-CimInstance Win32_Process -Filter ('ProcessId = ' + $current.ParentProcessId) -ErrorAction SilentlyContinue; if (-not $parent) { break }; $isNpmDev = $parent.CommandLine -like '*npm-cli.js*' -and $parent.CommandLine -like '*run*dev*'; $isNpmStart = $parent.CommandLine -like '*npm-cli.js*' -and $parent.CommandLine -like '*start*'; if ($isNpmDev -or $isNpmStart) { $target = $parent; break }; $current = $parent };" ^
  "if (-not $target) { Write-Error 'Could not safely identify this project npm server process.'; exit 3 };" ^
  "Write-Host ('Stopping this project server tree (PID ' + $target.ProcessId + ')...'); taskkill.exe /PID $target.ProcessId /T /F | Out-Null; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }"

if errorlevel 1 (
  echo Restart aborted.
  pause
  exit /b 1
)

powershell.exe -NoProfile -Command "Start-Sleep -Seconds 2"

echo [2/3] Creating a production build...
call npm run build

if errorlevel 1 (
  echo Production build failed. Server was not started.
  pause
  exit /b 1
)

echo [3/3] Starting Next.js production server on http://localhost:4000 ...
call npm start

endlocal
```

### 6. `README.md`

접속 포트를 수정하고 운영 재시작 안내를 추가합니다.

#### 변경 전

```markdown
Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
```

#### 변경 후

````markdown
Open [http://localhost:4000](http://localhost:4000) with your browser to see the result.

## Production restart

Run `restart-server.bat` to rebuild the application and restart the production server on port 4000.

The equivalent commands are:

```bash
npm run build
npm start
```
````

## 적용 후 검증 명령

```powershell
npm ci
npm run typecheck
./restart-server.bat
```

서버가 실행된 후 다음 주소가 HTTP 200을 반환하는지 확인합니다.

```text
http://localhost:4000/api/health
http://localhost:3000/health
```
