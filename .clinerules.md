## 프로젝트 개요

**CLX 매뉴얼 자동생성기** — exBuilder6 프레임워크 기반 `.clx.js` 파일을 정적 분석하여 사용자 매뉴얼(HTML/MD)을 자동 생성하는 Next.js 16 기반 웹 애플리케이션입니다.

## 기술 스택

| 계층 | 기술 |
|------|------|
| 프레임워크 | Next.js 16 (App Router), React 19 |
| 언어 | TypeScript 5 (strict mode) |
| 스타일링 | Tailwind CSS 4 + shadcn/ui |
| 상태관리 | Zustand 5 |
| 폼 | react-hook-form + zod |
| DB | Supabase (PostgreSQL) |
| AI | GitHub Models API / VS Code Extension 프록시 |
| 패키지 매니저 | npm |

## 디렉토리 구조

```
src/
├── app/                  # Next.js App Router 페이지 및 API 라우트
│   ├── page.tsx          # 메인 대시보드
│   ├── api/
│   │   └── generate/     # 매뉴얼 생성 API (POST)
│   ├── generate/         # 생성 페이지
│   ├── result/           # 결과 페이지
│   ├── dictionary/       # 단어사전 관리 페이지
│   └── layout/           # 레이아웃 관리 페이지
├── components/           # 재사용 UI 컴포넌트
│   ├── ui/               # shadcn/ui 컴포넌트
│   ├── layout/           # 레이아웃 컴포넌트
│   ├── upload/           # 파일 업로드
│   ├── file-tree/        # 파일 트리
│   ├── generation/       # 생성 옵션 패널
│   └── result/           # 결과 표시 컴포넌트
├── lib/
│   ├── parser/           # CLX 파일 정적 분석 엔진
│   ├── ai/               # AI 호출, 프롬프트, 컨텍스트 보강
│   ├── output/           # HTML/Markdown 렌더러
│   ├── supabase/         # Supabase 클라이언트 및 쿼리
│   └── utils/            # 공통 유틸리티
├── stores/               # Zustand 스토어
├── hooks/                # React 커스텀 훅
└── types/                # TypeScript 타입 정의
docs/                     # PRD, 로드맵, 사용자 가이드
scripts/                  # 분석·검증·E2E 스크립트
sample/                   # 샘플 .clx.js 파일
supabase/
└── migrations/           # DB 마이그레이션
```

## TypeScript 가이드라인

### 경로 별칭
- `@/*` → `./src/*` (tsconfig paths 설정)

### 타입 정의
- 모든 타입은 `src/types/` 에서 관리
- 도메인별 파일 분리: `ai.ts`, `clx.ts`, `database.ts`, `udc.ts`, `file-tree.ts`, `manual.ts`
- `src/types/index.ts`에서 re-export

### import 순서
1. 외부 라이브러리 (react, next, zustand, supabase 등)
2. `@/types` (타입 import)
3. `@/` 로 시작하는 내부 모듈
4. 상대 경로 내부 모듈 (`./`)

### 타입 안전성
- strict mode 활성화 — `any` 사용 금지
- 함수 반환 타입 명시적 선언
- `as` 캐스팅 최소화 (Supabase 응답 타입 단언은 예외)

## 상태 관리 가이드라인

### Zustand 스토어
```typescript
export const useXxxStore = create<XxxState>()(
  persist(
    (set, get) => ({
      // state
      // actions
    }),
    { name: "storage-key", partialize: ... }
  )
);
```

- 인터페이스로 State 타입 명시
- persist 미들웨어로 localStorage 연동 (필요한 경우)
- 각 action은 `set`으로 불변 업데이트

## 컴포넌트 가이드라인

### "use client" 지시문
- 상태, 이벤트 핸들러, 브라우저 API를 사용하는 컴포넌트에만 명시
- 순수 표시 컴포넌트는 서버 컴포넌트로 유지

### shadcn/ui 사용
- `npx shadcn add <component>` 로 컴포넌트 추가
- `components/ui/` 에 설치됨
- 필요 시 `@/components/ui/xxx` 로 import

### 스타일링
- Tailwind CSS 클래스 우선 사용
- CSS 모듈은 복잡한 애니메이션 등 Tailwind로 해결 불가능할 때만 사용
- `clsx`/`cn()` 유틸리티로 조건부 클래스 조합

## CLX 파서 가이드라인

핵심 파서 모듈 (`src/lib/parser/`):

| 파일 | 역할 |
|------|------|
| `index.ts` | 메인 진입점 — `analyzeFile()`, `analyzeFiles()` |
| `headerParser.ts` | `[시스템명]`, `[업무흐름]` 등 주석 메타데이터 |
| `crudParser.ts` | PatisMenuTitleBar·PatisTitleBar의 CRUD 로직 |
| `validationParser.ts` | 필수값·유효성 검증 메시지 |
| `conditionGroupParser.ts` | SEARCHGROUP, CONDITIONGROUP 분석 |
| `infoGroupParser.ts` | INFOGROUP 분석 |
| `gridParser.ts` | 그리드 컬럼 정의 |
| `popupParser.ts` | 팝업 호출 코드 |
| `embAppParser.ts` | 임베디드 앱(탭 페이지) |
| `udcRegistry.ts` | UDC 레지스트리 (자동 생성 — 수동 편집 금지) |
| `udc-label-resolver.ts` | UDC 라벨 해석기 |
| `visibility.ts` | visible 속성 체크 |

### 파서 함수 규칙
1. 입력: `(content: string, ...)` — CLX 파일 전체 본문
2. 출력: 해당 타입의 구조화된 결과
3. 정규식 기반 — 실행 흐름 분석 없이 패턴 매칭
4. `src/types/clx.ts`의 타입 정의에 맞춰 반환

## 출력 포맷 가이드라인

### HTML (`src/lib/output/html-renderer.ts`)
- `renderHtml()` 함수로 완전한 독립형 HTML 생성
- Tailwind Typography + 커스텀 스타일 인라인 포함
- 각 섹션: 개요 → 항목 → 그리드 → 사용방법 → 팝업
- `<script>` 태그로 탭 전환·아코디언 동작 포함

### Markdown (`src/lib/output/markdown-renderer.ts`)
- `renderMarkdown()` 함수로 GitHub Flavored Markdown 생성
- 표, 목록, 코드 블록 지원

## API 라우트 가이드라인

### 매뉴얼 생성 (`src/app/api/generate/route.ts`)
```typescript
export async function POST(request: NextRequest) {
  // 1. formData에서 파일 추출
  // 2. parse 요청 → 응답을 SSE로 스트리밍
  // 3. AI 호출 → 단어사전 조회/저장 → 결과 조립
  // 4. 최종 결과 반환
}
```

## 데이터베이스 가이드라인

### Supabase 쿼리 (`src/lib/supabase/`)
- 쿼리는 `src/lib/supabase/queries/` 에 도메인별 파일로 작성
- 클라이언트는 `src/lib/supabase/client.ts` 의 싱글톤 `supabase` 사용
- `.env.local` 의 환경변수로 연결 정보 관리

## 테스트 가이드라인

### E2E 스크립트 (`scripts/e2e-*.mjs`)
- `tsx` 러너로 실행 (`npx tsx scripts/e2e-xxx.mjs`)
- `scripts/verify-*.mjs` — 특정 기능 검증
- `scripts/test-*.mjs` — 단위 테스트 성격의 검증
- `scripts/analyze-*.mjs` — 분석 스크립트

### 검증 방법
1. `sample/` 디렉토리의 `.clx.js` 파일로 수동 검증
2. `npx tsc --noEmit` 으로 타입 체크
3. `npm run lint` 로 ESLint 검사
4. 해당 verify 스크립트 실행

## 일반 규칙

1. **기존 코드 스타일 유지** — 들여쓰기(탭), 작은따옴표/큰따옴표, 세미콜론 등 프로젝트 기존 패턴 준수
2. **TypeScript strict** — 모든 코드는 strict mode 통과해야 함
3. **주석은 한국어** — 코드 주석과 문서는 한국어로 작성
4. **함수는 작게** — 단일 책임 원칙, 100줄 이상 함수 지양
5. **매직 넘버 금지** — 상수는 의미 있는 이름으로 선언
6. **early return** — 깊은 중첩 대신 early return 패턴 사용
7. **nullable 체크** — `??`, `?.`, `if (!x) return` 등으로 방어적 코딩